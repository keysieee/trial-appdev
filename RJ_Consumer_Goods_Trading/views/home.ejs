<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            height: 100vh; /* Full viewport height */
            display: flex;
            color: black;
            background: url('images/background.png');
        }
        .container {
            display: flex;
            width: 100%;
            height: 100%;
        }
        .sidebar {
            width: 15%; /* Sidebar takes 1/4 of the viewport */
            background: darkgray; /* Dark gray background for sidebar */
            padding: 20px;
            color: white; /* Sidebar text color */
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2); /* Subtle shadow effect */
            display: flex;
            flex-direction: column; /* Stack items vertically */
        }
        .sidebar a {
            display: block; /* Block display for links */
            padding: 10px;
            color: white; /* Link color in sidebar */
            text-decoration: none;
            margin-bottom: 10px;
            border: 1px solid #ccc; /* Border for buttons */
            border-radius: 5px; /* Rounded corners */
            transition: background 0.3s; /* Smooth background change */
        }
        .sidebar a:hover {
            background: rgba(255, 0, 0, 0.2); /* Highlight on hover */
        }
        .main-content {
            flex-grow: 1; /* Allow main content to grow */
            display: flex;
            flex-direction: column; /* Stack header and content vertically */
            padding: 20px;
        }
        header {
            width: 100%;
            display: flex;
            justify-content: center; /* Center align store name */
            align-items: center;
            padding: 10px; /* Increased padding for more space */
            background: lightgray; /* Background for header */
            z-index: 1; /* Ensure header is above the background */
        }
        .store-name {
            font-size: 24px;
            font-weight: bold;
            color: black; /* Store name color */
            display: flex; /* Use flexbox to align items */
            align-items: center; /* Center items vertically */
            white-space: nowrap; /* Prevent line breaks */
        }
        .store-name img {
            height: 40px; /* Set logo height */
            margin-right: 10px; /* Space between logo and text */
        }
        .content {
            flex-grow: 1; /* Allow content area to grow */
            padding: 20px;
            color: black; /* Content text color */
            background: white; /* Plain white background for content */
            box-shadow: inset 0 0 5px rgba(0, 0, 0, 0.1); /* Subtle inner shadow */
            border-radius: 5px; /* Rounded corners */
            margin-top: 20px; /* Space between header and content */
            display: flex;
            justify-content: center; /* Center content horizontally */
            align-items: center; /* Center content vertically */
            flex-direction: column; /* Stack items vertically */
        }
        footer {
            width: 100%;
            background: darkgray; /* Dark gray background for footer */
            padding: 15px; /* Space around footer */
            color: black; /* Footer text color */
            display: flex;
            justify-content: flex-start; /* Align items to the left */
            position: relative; /* Position relative to the content */
        }
        .logout-button {
            background-color: darkred; /* Red background for logout button */
            color: white; /* Text color */
            padding: 10px; /* Padding for button */
            border: none; /* Remove border */
            border-radius: 5px; /* Rounded corners */
            cursor: pointer; /* Pointer cursor on hover */
            margin-top: 10px; /* Space above the button */
        }
        .logout-button:hover {
            opacity: 0.8; /* Slight transparency on hover */
        }
        .content img {
            max-width: 100%; /* Make image responsive */
            height: auto; /* Maintain aspect ratio */
            border-radius: 5px; /* Rounded corners for images */
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="sidebar">
            <div class="store-name">
                <img src="images/logo.png" alt="R&J Logo"> <!-- Replace with the actual path to your logo -->
                R&J Consumer Goods Trading
            </div>
            <br>
           <a href="/tasks" class="menu-item">Tasks</a>
                <a href="/inout" class="menu-item active">In/Out</a>
                <a href="/services" class="menu-item">Services</a>
                <a href="/inventory" class="menu-item">Inventory</a>
                <a href="/empshop" class="menu-item">Shop</a>
            <button class="logout-button" onclick="window.location.href='/logout'">Logout</button>
        </div>
        <div class="main-content">
            <header>
                <h1>Welcome Home</h1> <!-- You can customize this heading -->
            </header>
            <div class="content">
                <h2><img src="images/home.png" alt="Home Image"> </h2>
             <!-- Replace with the path to your image -->
            </div>
        </div>
    </div>
</body>
</html>
